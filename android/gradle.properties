# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

# Whether the app is configured to use edge-to-edge via the app config or `react-native-edge-to-edge` plugin
expo.edgeToEdgeEnabled=true
# Google Play rejects an artifact below its current target-API bar, and on
# 2026-09-10 it rejected this one:
#
#   Google Api Error: Invalid request - Target SDK of artifact is too low
#
# The build was fine; `eas submit` was not. Nothing in this repo had ever set a
# target: `android/app/build.gradle` reads `rootProject.ext.targetSdkVersion`,
# which `ExpoRootProjectPlugin` fills from the `expoLibs` catalogue, which falls
# through to `react-native/gradle/libs.versions.toml` -- `targetSdk = "35"`.
# That was the 2025 bar. The deadline is 31 August every year, and 35 stopped
# being enough ten days before this was written.
#
# Set *here* rather than through `expo-build-properties`, which looks like the
# tidier answer and would do nothing at all: `android/` is committed, so EAS
# builds this directory and never runs `prebuild` -- which is the only thing
# that plugin writes during. `ExpoAutolinkingSettingsExtension` maps these exact
# property names onto the catalogue, so they win from here.
android.targetSdkVersion=36
android.compileSdkVersion=36

# AGP 8.8.2 (from the React Native catalogue) tops out at compileSdk 35; 36
# wants AGP 8.9+. This is the documented escape hatch, and it is a stopgap
# rather than a fix -- the real one is Expo SDK 54, which ships an AGP that
# knows about 36. See ROADMAP, "Next".
android.suppressUnsupportedCompileSdk=36
